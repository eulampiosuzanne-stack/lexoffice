import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "jsr:@supabase/supabase-js@2";
const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"POST, OPTIONS"};
const json=(b:any,s=200)=>new Response(JSON.stringify(b),{status:s,headers:{...cors,"Content-Type":"application/json","Cache-Control":"no-store"}});const clean=(v:any)=>String(v??'').trim();
Deno.serve(async(req)=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});if(req.method!=='POST')return json({error:'Método não permitido.'},405);try{const url=Deno.env.get('SUPABASE_URL')!,anon=Deno.env.get('SUPABASE_ANON_KEY')!,service=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!;const auth=req.headers.get('Authorization')||'';const uc=createClient(url,anon,{global:{headers:{Authorization:auth}}});const {data:{user},error:userError}=await uc.auth.getUser();if(userError||!user)return json({error:'Sessão inválida. Faça login novamente.'},401);const db=createClient(url,service,{auth:{persistSession:false,autoRefreshToken:false}});const {data:profile}=await db.from('profiles').select('org_id').eq('id',user.id).maybeSingle();if(!profile?.org_id)return json({error:'Organização não encontrada.'},403);const orgId=profile.org_id;const body=await req.json().catch(()=>({}));const action=clean(body.action||'status');
if(action==='status'){const {data:connections,error:ce}=await db.from('tribunal_connections').select('id').eq('org_id',orgId).eq('user_id',user.id);if(ce)throw ce;const ids=(connections||[]).map((x:any)=>x.id);if(!ids.length)return json({ok:true,jobs:[],interventions:[]});const[{data:jobs,error:je},{data:interventions,error:ie}]=await Promise.all([db.from('tribunal_sync_jobs').select('*').eq('org_id',orgId).in('connection_id',ids).order('created_at',{ascending:false}).limit(100),db.from('tribunal_worker_interventions').select('*').eq('org_id',orgId).in('connection_id',ids).order('created_at',{ascending:false}).limit(100)]);if(je)throw je;if(ie)throw ie;return json({ok:true,jobs:jobs||[],interventions:interventions||[]});}
if(action==='save_credentials'||action==='verify_2fa'){const tribunalCode=clean(body.tribunal_code).toUpperCase(),judicialSystem=clean(body.judicial_system),username=clean(body.username||body.login),password=String(body.password||''),oabNumber=clean(body.oab_number),oabUf=clean(body.oab_uf).toUpperCase(),twoFactorCode=clean(body.two_factor_code);if(!tribunalCode||!judicialSystem||!username||!password)return json({error:'Informe tribunal, sistema, login e senha.'},400);if(action==='verify_2fa'&&!/^\d{6}$/.test(twoFactorCode))return json({error:'Informe o código atual de 6 dígitos do Google Authenticator.'},400);let {data:conn}=await db.from('tribunal_connections').select('*').eq('org_id',orgId).eq('user_id',user.id).eq('tribunal_code',tribunalCode).eq('judicial_system',judicialSystem).order('updated_at',{ascending:false}).limit(1).maybeSingle();if(!conn){const ins=await db.from('tribunal_connections').insert({org_id:orgId,user_id:user.id,tribunal_code:tribunalCode,tribunal_name:clean(body.tribunal_name)||tribunalCode,judicial_system:judicialSystem,oab_number:oabNumber||null,oab_uf:oabUf||null,status:'requires_auth',requires_2fa:action==='verify_2fa'||Boolean(body.requires_2fa),updated_at:new Date().toISOString()}).select('*').single();if(ins.error)throw ins.error;conn=ins.data;}else{const up=await db.from('tribunal_connections').update({oab_number:oabNumber||conn.oab_number||null,oab_uf:oabUf||conn.oab_uf||null,requires_2fa:action==='verify_2fa'||Boolean(body.requires_2fa??conn.requires_2fa),status:action==='verify_2fa'?'requires_2fa':'requires_auth',error_message:null,updated_at:new Date().toISOString()}).eq('id',conn.id).eq('user_id',user.id).select('*').single();if(up.error)throw up.error;conn=up.data;}const secret=JSON.stringify({username,password,tribunal_code:tribunalCode,judicial_system:judicialSystem});const {error:se}=await db.rpc('store_tribunal_connection_secret',{p_org_id:orgId,p_connection_id:conn.id,p_secret_value:secret});if(se)throw se;
if(action==='verify_2fa'){const {data:job,error:je}=await db.from('tribunal_sync_jobs').insert({org_id:orgId,connection_id:conn.id,job_type:'verify_2fa',status:'queued',search_payload:{requested_by:user.id,tribunal_code:tribunalCode,judicial_system:judicialSystem,two_factor_code:twoFactorCode,ephemeral_2fa:true},attempts:0,max_attempts:1,run_at:new Date().toISOString()}).select('id,status').single();if(je)throw je;return json({ok:true,connection_id:conn.id,job_id:job.id,status:'requires_2fa',message:'Código 2FA recebido e enviado para validação. A conexão só será marcada como conectada após confirmação real do sistema judicial.'});}
const requires2fa=Boolean(body.requires_2fa??conn.requires_2fa);await db.from('tribunal_connections').update({status:requires2fa?'requires_2fa':'requires_auth',error_message:null,updated_at:new Date().toISOString()}).eq('id',conn.id).eq('user_id',user.id);return json({ok:true,connection_id:conn.id,credentials_saved:true,message:requires2fa?'Acesso salvo. Informe o código atual do Google Authenticator para validar o 2FA.':'Acesso salvo. A conexão será confirmada somente após autenticação real no sistema judicial.'});}
if(action==='enqueue'){const connectionId=clean(body.connection_id);if(!connectionId)return json({error:'Conexão do tribunal não informada.'},400);const {data:conn,error:ce}=await db.from('tribunal_connections').select('*').eq('id',connectionId).eq('org_id',orgId).eq('user_id',user.id).maybeSingle();if(ce)throw ce;if(!conn)return json({error:'Conexão do tribunal não encontrada para este usuário.'},404);const {data:existing}=await db.from('tribunal_sync_jobs').select('id,status').eq('org_id',orgId).eq('connection_id',connectionId).in('status',['queued','processing']).order('created_at',{ascending:false}).limit(1).maybeSingle();if(existing)return json({ok:true,job_id:existing.id,status:existing.status,message:'Já existe uma atualização deste tribunal em processamento.'});const {data:job,error}=await db.from('tribunal_sync_jobs').insert({org_id:orgId,connection_id:connectionId,job_type:clean(body.job_type)||'sync',status:'queued',search_payload:{requested_by:user.id,tribunal_code:conn.tribunal_code,judicial_system:conn.judicial_system},attempts:0,max_attempts:3,run_at:new Date().toISOString()}).select('*').single();if(error)throw error;return json({ok:true,job_id:job.id,status:job.status,message:'Atualização processual solicitada.'});}return json({error:'Ação inválida.'},400);}catch(e){console.error('tribunal-worker-config',e);return json({error:e instanceof Error?e.message:'Erro inesperado.'},500)}});