import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "jsr:@supabase/supabase-js@2";
const cors={"Access-Control-Allow-Origin":"*","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"POST, OPTIONS"};
const json=(b:any,s=200)=>new Response(JSON.stringify(b),{status:s,headers:{...cors,"content-type":"application/json; charset=utf-8","cache-control":"no-store"}});
const admin=()=>createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!,{auth:{persistSession:false}});
async function resolveOrg(requested:string){if(requested)return requested;try{const {data}=await admin().from('ai_agent_policies').select('org_id').eq('active',true).contains('allowed_channels',['whatsapp']).limit(1).maybeSingle();return String(data?.org_id||'')}catch{return''}}
async function vaultKey(orgId:string,provider:string){try{if(!orgId)return'';const {data}=await admin().rpc('read_integration_secret',{p_org_id:orgId,p_provider:provider});return String(data||'').trim()}catch{return''}}
async function openai(key:string,sys:string,input:string,modelHint:string){if(!key)return{ok:false,error:'openai_key_missing'};const model=modelHint?.startsWith('gpt-')?modelHint:(Deno.env.get('OPENAI_MODEL')||'gpt-5-mini');try{const r=await fetch('https://api.openai.com/v1/responses',{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json'},body:JSON.stringify({model,instructions:sys,input,max_output_tokens:1400})});const d=await r.json().catch(()=>null);if(!r.ok)return{ok:false,error:`openai_${r.status}`,detail:d?.error?.message||null};const text=String(d?.output_text||d?.output?.flatMap((x:any)=>x.content||[]).map((x:any)=>x.text||'').join('')||'').trim();return text?{ok:true,provider:'openai',model,text}:{ok:false,error:'openai_empty'}}catch(e){return{ok:false,error:'openai_network',detail:e instanceof Error?e.message:String(e)}}}
async function gemini(key:string,sys:string,input:string,modelHint:string){if(!key)return{ok:false,error:'gemini_key_missing'};const model=modelHint?.startsWith('gemini-')?modelHint:(Deno.env.get('GEMINI_MODEL')||'gemini-2.5-flash');try{const r=await fetch(`https://generativelanguage.googleapis.com/v1beta/models/${model}:generateContent?key=${encodeURIComponent(key)}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({systemInstruction:{parts:[{text:sys}]},contents:[{role:'user',parts:[{text:input}]}],generationConfig:{temperature:.25,maxOutputTokens:1400}})});const d=await r.json().catch(()=>null);if(!r.ok)return{ok:false,error:`gemini_${r.status}`,detail:d?.error?.message||null};const text=String(d?.candidates?.[0]?.content?.parts?.map((p:any)=>p.text||'').join('')||'').trim();return text?{ok:true,provider:'gemini',model,text}:{ok:false,error:'gemini_empty'}}catch(e){return{ok:false,error:'gemini_network',detail:e instanceof Error?e.message:String(e)}}}
async function compat(name:string,key:string,url:string,model:string,sys:string,input:string){if(!key)return{ok:false,error:`${name}_key_missing`};try{const r=await fetch(url,{method:'POST',headers:{Authorization:`Bearer ${key}`,'Content-Type':'application/json',...(name==='openrouter'?{'HTTP-Referer':'https://lexoffice-ashy.vercel.app','X-Title':'LEXOFFICE'}:{})},body:JSON.stringify({model,messages:[{role:'system',content:sys},{role:'user',content:input}],temperature:.25,max_tokens:1400})});const d=await r.json().catch(()=>null);if(!r.ok)return{ok:false,error:`${name}_${r.status}`,detail:d?.error?.message||null};const text=String(d?.choices?.[0]?.message?.content||'').trim();return text?{ok:true,provider:name,model,text}:{ok:false,error:`${name}_empty`}}catch(e){return{ok:false,error:`${name}_network`,detail:e instanceof Error?e.message:String(e)}}}
async function ollama(base:string,sys:string,input:string){if(!base)return{ok:false,error:'ollama_url_missing'};const model=Deno.env.get('OLLAMA_MODEL')||'llama3.2:3b';try{const r=await fetch(`${base.replace(/\/$/,'')}/api/chat`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({model,stream:false,messages:[{role:'system',content:sys},{role:'user',content:input}]})});const d=await r.json().catch(()=>null);if(!r.ok)return{ok:false,error:`ollama_${r.status}`};const text=String(d?.message?.content||'').trim();return text?{ok:true,provider:'ollama',model,text}:{ok:false,error:'ollama_empty'}}catch(e){return{ok:false,error:'ollama_network',detail:e instanceof Error?e.message:String(e)}}}
Deno.serve(async req=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});if(req.method!=='POST')return json({error:'method_not_allowed'},405);const body=await req.json().catch(()=>({}));const orgId=await resolveOrg(String(body.org_id||'').trim());const [voai,vge,vg,vor,vol]=await Promise.all([vaultKey(orgId,'openai_api'),vaultKey(orgId,'gemini_api'),vaultKey(orgId,'groq_api'),vaultKey(orgId,'openrouter_api'),vaultKey(orgId,'ollama_base_url')]);const keys={openai:voai||(Deno.env.get('OPENAI_API_KEY')||'').trim(),gemini:vge||(Deno.env.get('GEMINI_API_KEY')||Deno.env.get('GOOGLE_GEMINI_API_KEY')||'').trim(),groq:(Deno.env.get('GROQ_API_KEY')||'').trim()||vg,openrouter:vor||(Deno.env.get('OPENROUTER_API_KEY')||'').trim(),ollama:vol||(Deno.env.get('OLLAMA_BASE_URL')||'').trim()};if(body.action==='health')return json({ok:Object.values(keys).some(Boolean),mode:'free_first_with_paid_fallback',groq_configured:Boolean(keys.groq),gemini_configured:Boolean(keys.gemini),openrouter_configured:Boolean(keys.openrouter),ollama_configured:Boolean(keys.ollama),openai_configured:Boolean(keys.openai),providers_ready:Object.values(keys).filter(Boolean).length});const sys=String(body.instructions||'Você é um agente do LEXOFFICE. Responda em português do Brasil, com clareza, cordialidade e objetividade. Nunca invente dados jurídicos, prazos, processos, valores ou compromissos. Se faltar informação essencial, peça-a ou encaminhe para atendimento humano.'),input=String(body.input||'');if(!input.trim())return json({error:'input_required'},400);const requested=String(body.provider||'auto').toLowerCase(),hint=String(body.model||'');let order=['groq','gemini','openrouter','ollama','openai'];if(requested!=='auto'&&order.includes(requested))order=[requested,...order.filter(x=>x!==requested)];const errors:any[]=[];for(const p of order){let r:any;if(p==='groq')r=await compat('groq',keys.groq,'https://api.groq.com/openai/v1/chat/completions',Deno.env.get('GROQ_MODEL')||'openai/gpt-oss-20b',sys,input);else if(p==='gemini')r=await gemini(keys.gemini,sys,input,hint);else if(p==='openrouter')r=await compat('openrouter',keys.openrouter,'https://openrouter.ai/api/v1/chat/completions',Deno.env.get('OPENROUTER_MODEL')||'openrouter/free',sys,input);else if(p==='ollama')r=await ollama(keys.ollama,sys,input);else r=await openai(keys.openai,sys,input,hint);if(r.ok)return json({ok:true,...r,fallback_order:order});errors.push(r)}return json({ok:false,error:'all_providers_failed',providers:errors.map(x=>({error:x.error,detail:x.detail||null}))},503)});