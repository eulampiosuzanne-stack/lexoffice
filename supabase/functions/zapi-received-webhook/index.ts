import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import {createClient} from "jsr:@supabase/supabase-js@2.57.4";
const J=(x:any,s=200)=>new Response(JSON.stringify(x),{status:s,headers:{"content-type":"application/json"}});
const A=()=>createClient(Deno.env.get("SUPABASE_URL")!,Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!);
async function S(a:any,k:string){const {data}=await a.from("system_runtime_secrets").select("secret").eq("key",k).maybeSingle();return String(data?.secret||"").trim()}
async function H(v:string){const b=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(v));return [...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,"0")).join("")}
Deno.serve(async req=>{if(req.method!=="POST")return J({ok:false},405);try{const body=await req.json().catch(()=>null);if(!body)return J({ok:true,ignored:"empty"});const a=A();const expected=await S(a,"zapi_instance_id");const received=String(body?.instanceId||body?.instance?.id||"");if(expected&&received&&expected!==received)return J({ok:true,ignored:"instance"});const token=await S(a,"zapi_client_token");if(!token)return J({ok:false,error:"zapi_not_configured"},503);const key=await H(token);const r=await fetch(`${Deno.env.get("SUPABASE_URL")}/functions/v1/zapi-message-webhook?key=${key}`,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(body)});const t=await r.text();let d:any;try{d=JSON.parse(t)}catch{d={response:t}}return J({ok:r.ok,relayed:true,upstream:d},r.ok?200:r.status)}catch(e){console.error(e);return J({ok:false,error:e instanceof Error?e.message:String(e)},500)}});
