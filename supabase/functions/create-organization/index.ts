import "jsr:@supabase/functions-js/edge-runtime.d.ts";
import { createClient } from "jsr:@supabase/supabase-js@2";

type Payload = { action?:string; name?:string; org_name?:string; email?:string; password?:string; org_id?:string; user_id?:string; role_key?:string; status?:string };
const allowedOrigins=new Set(["https://lexoffice-ashy.vercel.app","http://localhost:5173","http://127.0.0.1:5173"]);
function corsHeaders(req:Request){const origin=req.headers.get("origin")||"";const allowOrigin=allowedOrigins.has(origin)?origin:"https://lexoffice-ashy.vercel.app";return{"Access-Control-Allow-Origin":allowOrigin,"Vary":"Origin","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"POST, OPTIONS"}}
function jsonResponse(req:Request,body:Record<string,unknown>,status=200){return new Response(JSON.stringify(body),{status,headers:{...corsHeaders(req),"Content-Type":"application/json","Cache-Control":"no-store"}})}
function slugify(value:string){return value.normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase().trim().replace(/[^a-z0-9]+/g,"-").replace(/^-+|-+$/g,"")}
Deno.serve(async(req)=>{if(req.method==="OPTIONS"){const origin=req.headers.get("origin")||"";if(origin&&!allowedOrigins.has(origin))return new Response("Origin not allowed",{status:403});return new Response("ok",{status:200,headers:corsHeaders(req)})}if(req.method!=="POST")return jsonResponse(req,{error:"Method not allowed"},405);const origin=req.headers.get("origin")||"";if(origin&&!allowedOrigins.has(origin))return jsonResponse(req,{error:"Origin not allowed"},403);
try{const supabaseUrl=Deno.env.get("SUPABASE_URL"),serviceRoleKey=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY");if(!supabaseUrl||!serviceRoleKey)return jsonResponse(req,{error:"Configuração do Supabase ausente."},500);const authHeader=req.headers.get("Authorization")||"";if(!authHeader.startsWith("Bearer "))return jsonResponse(req,{error:"Não autenticado."},401);const admin=createClient(supabaseUrl,serviceRoleKey,{auth:{autoRefreshToken:false,persistSession:false}});const token=authHeader.slice(7).trim();const {data:authData,error:authError}=await admin.auth.getUser(token);const caller=authData?.user;if(authError||!caller)return jsonResponse(req,{error:"Sessão inválida."},401);
const {data:callerProfile}=await admin.from("profiles").select("id,org_id,status").eq("id",caller.id).maybeSingle();if(!callerProfile||callerProfile.status!=="active")return jsonResponse(req,{error:"Usuário sem perfil ativo."},403);const {data:platformAdmin}=await admin.from("platform_admins").select("user_id,status").eq("user_id",caller.id).eq("status","active").maybeSingle();if(!platformAdmin)return jsonResponse(req,{error:"Acesso restrito ao administrador da plataforma."},403);
const body=(await req.json()) as Payload;const action=body.action||"create_organization";
if(action==="create_user"){const{name,email,password,org_id,role_key="assistant"}=body;if(!name||!email||!password||!org_id)return jsonResponse(req,{error:"Campos obrigatórios ausentes."},400);if(password.length<8)return jsonResponse(req,{error:"A senha deve ter pelo menos 8 caracteres."},400);const normalizedEmail=email.trim().toLowerCase();const{data:existing}=await admin.from("profiles").select("id").eq("email",normalizedEmail).maybeSingle();if(existing)return jsonResponse(req,{error:"Já existe um usuário com este e-mail."},409);let{data:role}=await admin.from("roles").select("id").eq("org_id",org_id).eq("key",role_key).maybeSingle();if(!role){const names:any={owner:"Proprietário",lawyer:"Advogado(a)",assistant:"Profissional do escritório"};const rr=await admin.from("roles").insert({org_id,key:role_key,name:names[role_key]||role_key,is_system:role_key!=="assistant"}).select("id").single();if(rr.error)return jsonResponse(req,{error:rr.error.message},400);role=rr.data}const u=await admin.auth.admin.createUser({email:normalizedEmail,password,email_confirm:true,user_metadata:{name:name.trim()}});if(u.error||!u.data.user)return jsonResponse(req,{error:u.error?.message||"Falha ao criar usuário."},400);const uid=u.data.user.id;const ins=await admin.from("profiles").insert({id:uid,org_id,role_id:role.id,role_key,name:name.trim(),email:normalizedEmail,status:"active"});if(ins.error){await admin.auth.admin.deleteUser(uid);return jsonResponse(req,{error:ins.error.message},400)}await admin.from("audit_logs").insert({org_id:callerProfile.org_id,user_id:caller.id,action:"platform.user.create",entity:"profile",entity_id:uid,metadata:{target_org_id:org_id,email:normalizedEmail,role_key}});return jsonResponse(req,{ok:true,user_id:uid},201)}
if(action==="update_user"){const{user_id,org_id,role_key,status}=body;if(!user_id)return jsonResponse(req,{error:"Usuário não informado."},400);const{data:target}=await admin.from("profiles").select("id,org_id,role_key,status").eq("id",user_id).maybeSingle();if(!target)return jsonResponse(req,{error:"Usuário não encontrado."},404);const nextOrg=org_id||target.org_id,nextRole=role_key||target.role_key,nextStatus=status||target.status;if(target.role_key==="owner"&&(nextOrg!==target.org_id||nextRole!=="owner"||nextStatus!=="active")){const{count}=await admin.from("profiles").select("*",{count:"exact",head:true}).eq("org_id",target.org_id).eq("role_key","owner").eq("status","active");if((count||0)<=1)return jsonResponse(req,{error:"A empresa precisa manter pelo menos um proprietário ativo."},409)}let{data:role}=await admin.from("roles").select("id").eq("org_id",nextOrg).eq("key",nextRole).maybeSingle();if(!role){const names:any={owner:"Proprietário",lawyer:"Advogado(a)",assistant:"Profissional do escritório"};const rr=await admin.from("roles").insert({org_id:nextOrg,key:nextRole,name:names[nextRole]||nextRole,is_system:nextRole!=="assistant"}).select("id").single();if(rr.error)return jsonResponse(req,{error:rr.error.message},400);role=rr.data}const up=await admin.from("profiles").update({org_id:nextOrg,role_id:role.id,role_key:nextRole,status:nextStatus}).eq("id",user_id);if(up.error)return jsonResponse(req,{error:up.error.message},400);await admin.from("audit_logs").insert({org_id:callerProfile.org_id,user_id:caller.id,action:"platform.user.update",entity:"profile",entity_id:user_id,metadata:{before:target,after:{org_id:nextOrg,role_key:nextRole,status:nextStatus}}});return jsonResponse(req,{ok:true})}
const{name,org_name,email,password}=body;if(!name||!org_name||!email||!password)return jsonResponse(req,{error:"Campos obrigatórios ausentes."},400);if(password.length<8)return jsonResponse(req,{error:"A senha deve ter pelo menos 8 caracteres."},400);const normalizedEmail=email.trim().toLowerCase(),baseSlug=slugify(org_name)||"escritorio";const {data:existingProfile}=await admin.from("profiles").select("id").eq("email",normalizedEmail).maybeSingle();if(existingProfile)return jsonResponse(req,{error:"Já existe um usuário com este e-mail."},409);
const {data:userData,error:userError}=await admin.auth.admin.createUser({email:normalizedEmail,password,email_confirm:true,user_metadata:{name:name.trim()}});if(userError||!userData.user)return jsonResponse(req,{error:userError?.message??"Falha ao criar usuário."},400);const userId=userData.user.id,uniqueSlug=`${baseSlug}-${crypto.randomUUID().slice(0,8)}`;const {data:org,error:orgError}=await admin.from("organizations").insert({name:org_name.trim(),slug:uniqueSlug,plan:"starter",status:"active",settings:{}}).select("id").single();if(orgError||!org){await admin.auth.admin.deleteUser(userId);return jsonResponse(req,{error:orgError?.message??"Falha ao criar organização."},400)}const {data:role,error:roleError}=await admin.from("roles").insert({org_id:org.id,key:"owner",name:"Proprietário",is_system:true}).select("id").single();if(roleError||!role){await admin.from("organizations").delete().eq("id",org.id);await admin.auth.admin.deleteUser(userId);return jsonResponse(req,{error:roleError?.message??"Falha ao criar cargo."},400)}const {error:profileError}=await admin.from("profiles").insert({id:userId,org_id:org.id,role_id:role.id,role_key:"owner",name:name.trim(),email:normalizedEmail,status:"active"});if(profileError){await admin.from("roles").delete().eq("id",role.id);await admin.from("organizations").delete().eq("id",org.id);await admin.auth.admin.deleteUser(userId);return jsonResponse(req,{error:profileError.message},400)}
await admin.from("audit_logs").insert({org_id:callerProfile.org_id,user_id:caller.id,action:"organization.create",entity:"organization",entity_id:org.id,metadata:{created_org_id:org.id,created_user_id:userId,created_email:normalizedEmail}});return jsonResponse(req,{ok:true,user_id:userId,org_id:org.id},201)}catch(error){console.error("create-organization",error);return jsonResponse(req,{error:"Erro interno."},500)}});