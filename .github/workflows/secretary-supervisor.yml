name: Lex Secretary Supervisor (legacy disabled)

# The permanent dispatcher now runs inside Supabase every five minutes.
# This workflow remains manual-only for audit history and must not call production.
on:
  workflow_dispatch:

jobs:
  legacy-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Dispatcher status
        run: echo "Scheduled supervision is handled by the secure Lex Executive Dispatcher in Supabase."
