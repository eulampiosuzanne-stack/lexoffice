import { useEffect,useMemo,useState } from 'react';
import { Send,PenTool,RefreshCw,ShieldCheck,KeyRound,FileText,UserRound,Plus,Usb,CheckCircle2,AlertTriangle } from 'lucide-react';
import { supabase } from '../lib/supabase';
import './integrations.css';
type RequestRow={id:string;title?:string|null;status?:string|null;external_id?:string|null;sent_at?:string|null;expires_at?:string|null;document_id?:string|null;created_at?:string|null};
type DocumentRow={id:string;name:string;file_path?:string|null;client_id?:string|null;process_id?:string|null;mime_type?:string|null;created_at?:string|null};type ClientRow={id:string;name:string;email?:string|null;phone?:string|null;cpf_cnpj?:string|null};type LocalCert={subject:string;issuer:string;fingerprint:string;notBefore?:string;notAfter?:string};
const BRIDGE='http://127.0.0.1:17681';
export default function Signatures(){const [rows,setRows]=useState<RequestRow[]>([]),[documents,setDocuments]=useState<DocumentRow[]>([]),[clients,setClients]=useState<ClientRow[]>([]),[loading,setLoading]=useState(true),[busy,setBusy]=useState<string|null>(null),[notice,setNotice]=useState(''),[accountEmail,setAccountEmail]=useState(''),[token,setToken]=useState(''),[configured,setConfigured]=useState(false),[integrationStatus,setIntegrationStatus]=useState('não configurada');const [documentId,setDocumentId]=useState(''),[signerName,setSignerName]=useState(''),[signerEmail,setSignerEmail]=useState(''),[signerPhone,setSignerPhone]=useState(''),[expiresAt,setExpiresAt]=useState('');const [bridgeOnline,setBridgeOnline]=useState(false),[certs,setCerts]=useState<LocalCert[]>([]),[certFingerprint,setCertFingerprint]=useState(''),[visibleSeal,setVisibleSeal]=useState(true);const selectedDoc=useMemo(()=>documents.find(d=>d.id===documentId)||null,[documents,documentId]);
async function load(){if(!supabase)return;setLoading(true);setNotice('');const [{data,error},{data:docs},{data:cls},{data:cfg,error:cfgError}]=await Promise.all([supabase.from('signature_requests').select('id,title,status,external_id,sent_at,expires_at,document_id,created_at').order('created_at',{ascending:false}),supabase.from('documents').select('id,name,file_path,client_id,process_id,mime_type,created_at').eq('mime_type','application/pdf').order('created_at',{ascending:false}).limit(1000),supabase.from('clients').select('id,name,email,phone,cpf_cnpj').order('name').limit(1500),supabase.functions.invoke('zapsign-configure',{body:{action:'status'}})]);if(error)setNotice(error.message);else setRows((data||[])as RequestRow[]);setDocuments((docs||[])as DocumentRow[]);setClients((cls||[])as ClientRow[]);if(!cfgError&&cfg){setConfigured(Boolean((cfg as any).configured));setIntegrationStatus((cfg as any).status||'não configurada');setAccountEmail((cfg as any).account_email||'')}setLoading(false)}useEffect(()=>{load();checkBridge()},[]);useEffect(()=>{if(!selectedDoc?.client_id)return;const c=clients.find(x=>x.id===selectedDoc.client_id);if(c){setSignerName(c.name||'');setSignerEmail(c.email||'');setSignerPhone(c.phone||'')}},[selectedDoc,clients]);
async function checkBridge(){setBusy('bridge');try{const r=await fetch(`${BRIDGE}/health`,{signal:AbortSignal.timeout(1800)});if(!r.ok)throw new Error();setBridgeOnline(true);const cr=await fetch(`${BRIDGE}/certificates`,{signal:AbortSignal.timeout(2500)});if(cr.ok){const j=await cr.json();const list=Array.isArray(j)?j:(j.certificates||[]);setCerts(list);if(list.length&&!certFingerprint)setCertFingerprint(list[0].fingerprint)}}catch{setBridgeOnline(false);setCerts([])}finally{setBusy(null)}}
async function saveConfig(){if(!supabase||!token.trim())return;setBusy('config');setNotice('');const {data,error}=await supabase.functions.invoke('zapsign-configure',{body:{account_email:accountEmail.trim(),token:token.trim()}});if(error)setNotice(error.message);else if((data as any)?.error)setNotice((data as any).error);else{setConfigured(true);setIntegrationStatus('configured');setToken('');setNotice('ZapSign configurada. A credencial foi armazenada no cofre seguro do servidor.')}setBusy(null)}
async function addRequest(){if(!supabase||!selectedDoc||!signerName.trim())return;setBusy('add');setNotice('');const {data:{user}}=await supabase.auth.getUser();if(!user){setBusy(null);setNotice('Sessão expirada.');return}const {data:p}=await supabase.from('profiles').select('org_id').eq('id',user.id).maybeSingle();if(!p?.org_id){setBusy(null);setNotice('Organização não identificada.');return}const {data:req,error:re}=await supabase.from('signature_requests').insert({org_id:p.org_id,client_id:selectedDoc.client_id||null,process_id:selectedDoc.process_id||null,document_id:selectedDoc.id,title:selectedDoc.name,signer_name:signerName.trim(),signer_email:signerEmail.trim()||null,provider:'zapsign',status:'draft',expires_at:expiresAt?new Date(`${expiresAt}T23:59:59`).toISOString():null,created_by:user.id}).select('id').single();if(re||!req){setBusy(null);setNotice(re?.message||'Não foi possível criar a solicitação.');return}const {error:se}=await supabase.from('signature_signers').insert({org_id:p.org_id,signature_request_id:req.id,name:signerName.trim(),email:signerEmail.trim()||null,phone:signerPhone.trim()||null,signing_order:1,status:'pending'});if(se){await supabase.from('signature_requests').delete().eq('id',req.id);setBusy(null);setNotice(se.message);return}setNotice('Documento adicionado para assinatura.');setBusy(null);await load()}
async function send(id:string){if(!supabase)return;setBusy(id);setNotice('');const {data,error}=await supabase.functions.invoke('zapsign-send-signature',{body:{signature_request_id:id}});if(error)setNotice(error.message);else if((data as any)?.error)setNotice((data as any).error);else{setNotice('Documento enviado para assinatura pela ZapSign.');await load()}setBusy(null)}
async function signIcp(){if(!supabase||!selectedDoc?.file_path||!certFingerprint)return;setBusy('icp');setNotice('');try{if(!bridgeOnline)throw new Error('A ponte ICP-Brasil local não está ativa neste computador.');const {data:pdf,error}=await supabase.storage.from('lexoffice-documents').download(selectedDoc.file_path);if(error||!pdf)throw error||new Error('PDF não encontrado.');const form=new FormData();form.append('file',pdf,selectedDoc.name.replace(/\.[^.]+$/,'')+'.pdf');form.append('certificateFingerprint',certFingerprint);form.append('visibleSeal',String(visibleSeal));form.append('sealText','Certificado ICP-Brasil');const r=await fetch(`${BRIDGE}/sign/pades`,{method:'POST',body:form});if(!r.ok){const t=await r.text();throw new Error(t||'A assinatura local não foi concluída.')}const signed=await r.blob();if(!signed.size)throw new Error('O arquivo assinado retornou vazio.');const path=selectedDoc.file_path.replace(/\.pdf$/i,'')+`-assinado-icpbr-${Date.now()}.pdf`;const {error:up}=await supabase.storage.from('lexoffice-documents').upload(path,signed,{contentType:'application/pdf'});if(up)throw up;const {data:{user}}=await supabase.auth.getUser();const {data:p}=user?await supabase.from('profiles').select('org_id').eq('id',user.id).maybeSingle():({data:null}as any);if(!p?.org_id)throw new Error('Organização não identificada para registrar o PDF assinado.');const cert=certs.find(c=>c.fingerprint===certFingerprint);const {error:ie}=await supabase.from('documents').insert({org_id:p.org_id,client_id:selectedDoc.client_id||null,process_id:selectedDoc.process_id||null,name:selectedDoc.name.replace(/\.pdf$/i,'')+' — Assinado ICP-Brasil.pdf',file_path:path,mime_type:'application/pdf',category:'Documento assinado',notes:`PAdES ICP-Brasil • ${cert?.issuer||'certificado local'} • ${new Date().toLocaleString('pt-BR')}`,uploaded_by:user?.id||null});if(ie)throw ie;setNotice('PDF assinado com ICP-Brasil e salvo na biblioteca do LEXOFFICE.');await load()}catch(e:any){setNotice(e?.message||'Não foi possível assinar com ICP-Brasil.')}finally{setBusy(null)}}
return <><div className="page-title"><h1>Assinaturas</h1><p>Assinatura eletrônica pela ZapSign e assinatura digital PAdES com certificado ICP-Brasil A3.</p></div>{notice&&<div className="integration-notice">{notice}</div>}<div className="integration-panel"><h3><Usb size={17}/> Assinar com meu certificado ICP-Brasil A3</h3><p>O PIN e a chave privada permanecem neste computador e no token. O LEXOFFICE recebe somente o PDF assinado.</p><div className="integration-form"><label><FileText size={14}/> Documento PDF<select value={documentId} onChange={e=>setDocumentId(e.target.value)}><option value="">Selecione um documento</option>{documents.map(d=><option key={d.id} value={d.id}>{d.name}</option>)}</select></label><label>Status da ponte local<div style={{display:'flex',gap:8,alignItems:'center',minHeight:46}}>{bridgeOnline?<><CheckCircle2 size={18}/>Pronta</>:<><AlertTriangle size={18}/>Não iniciada</>}<button type="button" className="integration-action" onClick={checkBridge}>Verificar</button></div></label><label>Certificado<select value={certFingerprint} onChange={e=>setCertFingerprint(e.target.value)} disabled={!bridgeOnline}><option value="">Selecione</option>{certs.map(c=><option key={c.fingerprint} value={c.fingerprint}>{c.subject} • {c.issuer}</option>)}</select></label><label style={{display:'flex',gap:10,alignItems:'center'}}><input type="checkbox" checked={visibleSeal} onChange={e=>setVisibleSeal(e.target.checked)}/> Mostrar selo visual ICP-Brasil no PDF</label><button className="integration-action" onClick={signIcp} disabled={busy==='icp'||!documentId||!bridgeOnline||!certFingerprint}><ShieldCheck size={16}/>{busy==='icp'?'Assinando...':'Assinar PDF com ICP-Brasil'}</button></div></div><div className="integration-panel"><h3><Plus size={17}/> Enviar documento para assinatura eletrônica</h3><div className="integration-form"><label>Documento PDF<select value={documentId} onChange={e=>setDocumentId(e.target.value)}><option value="">Selecione</option>{documents.map(d=><option key={d.id} value={d.id}>{d.name}</option>)}</select></label><label><UserRound size={14}/> Signatário<input value={signerName} onChange={e=>setSignerName(e.target.value)}/></label><label>E-mail<input type="email" value={signerEmail} onChange={e=>setSignerEmail(e.target.value)}/></label><label>WhatsApp / telefone<input value={signerPhone} onChange={e=>setSignerPhone(e.target.value)}/></label><label>Prazo<input type="date" value={expiresAt} onChange={e=>setExpiresAt(e.target.value)}/></label><button className="integration-action" onClick={addRequest} disabled={busy==='add'||!documentId||!signerName.trim()}><PenTool size={16}/>Adicionar para assinatura</button></div></div><div className="integration-panel"><h3><KeyRound size={17}/> Configuração ZapSign</h3><div className="integration-form"><label>E-mail da conta<input type="email" value={accountEmail} onChange={e=>setAccountEmail(e.target.value)}/></label><label>Token / API key<input type="password" value={token} onChange={e=>setToken(e.target.value)} placeholder={configured?'•••••••• configurado':'Token ZapSign'}/></label><label>Status<input value={configured?`Configurada • ${integrationStatus}`:'Não configurada'} readOnly/></label><button className="integration-action" onClick={saveConfig} disabled={busy==='config'||!token.trim()}><ShieldCheck size={16}/>Salvar integração</button></div></div><div className="integration-panel"><h3>Solicitações ZapSign</h3>{loading?<p>Carregando...</p>:<div className="signature-list">{rows.map(r=><div className="signature-row" key={r.id}><div><strong>{r.title||'Documento'}</strong><small>Status: {r.status||'pendente'}</small></div><div className="signature-actions">{!r.external_id&&r.status!=='sent'&&r.status!=='signed'&&<button className="integration-action" disabled={busy===r.id||!configured} onClick={()=>send(r.id)}><Send size={15}/>Enviar ZapSign</button>}</div></div>)}</div>}</div></>}
