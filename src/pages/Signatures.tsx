import { useEffect, useMemo, useState } from 'react';
import { Activity, AlertTriangle, Bell, CheckCircle2, Download, FileCheck2, FileText, Fingerprint, KeyRound, RefreshCw, Send, ShieldCheck, Trash2, UploadCloud, UserRound, Wifi, WifiOff } from 'lucide-react';
import { supabase } from '../lib/supabase';
import './integrations.css';

type RequestRow = { id:string; title?:string|null; status?:string|null; external_id?:string|null; sent_at?:string|null; signed_at?:string|null; expires_at?:string|null; document_id?:string|null; client_id?:string|null; signer_name?:string|null; signer_phone?:string|null; created_at?:string|null; signed_document_id?:string|null; signed_file_captured_at?:string|null; icp_validated_document_id?:string|null; icp_validated_at?:string|null; verification_method?:string|null; verification_status?:string|null; delivery_email?:boolean|null; delivery_whatsapp?:boolean|null; batch_id?:string|null; batch_order?:number|null };
type DocumentRow = { id:string; name:string; file_path?:string|null; client_id?:string|null; process_id?:string|null; mime_type?:string|null; category?:string|null; created_at?:string|null };
type ClientRow = { id:string; name:string; email?:string|null; phone?:string|null; whatsapp?:string|null; cpf_cnpj?:string|null };
type LocalCert = { subject:string; issuer:string; fingerprint:string; notBefore?:string; notAfter?:string };
type ProviderStatus = { provider?:string; documenso:{configured:boolean;reachable:boolean;mode?:string;base_url?:string}; biometrics:{configured:boolean;reachable:boolean} };

const BRIDGE='http://127.0.0.1:17681';
const isSigned=(status?:string|null)=>(status||'').toLowerCase()==='signed';
const isWaiting=(status?:string|null)=>['sent','viewed','verification_pending'].includes((status||'').toLowerCase());
const safeName=(name:string)=>name.normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-zA-Z0-9._-]+/g,'-').replace(/^-+|-+$/g,'')||'documento.pdf';
const verificationOptions=[
 {value:'facial_document',label:'Biometria facial + documento',detail:'Verificação com rosto e RG, CIN ou CNH',biometric:true},
 {value:'facial',label:'Verificação facial — somente rosto',detail:'Captura facial sem exigir documento de identidade',biometric:true},
];
const verificationLabel=(method?:string|null)=>verificationOptions.find(i=>i.value===method)?.label||'Verificação facial';

export default function Signatures(){
 const [rows,setRows]=useState<RequestRow[]>([]),[documents,setDocuments]=useState<DocumentRow[]>([]),[clients,setClients]=useState<ClientRow[]>([]);
 const [loading,setLoading]=useState(true),[busy,setBusy]=useState<string|null>(null),[notice,setNotice]=useState('');
 const [recipientMode,setRecipientMode]=useState<'client'|'manual'>('client');
 const [selectedClientId,setSelectedClientId]=useState('');
 const [manualName,setManualName]=useState(''),[manualEmail,setManualEmail]=useState(''),[manualPhone,setManualPhone]=useState('');
 const [localFiles,setLocalFiles]=useState<File[]>([]),[existingDocumentIds,setExistingDocumentIds]=useState<string[]>([]);
 const [verificationMethod,setVerificationMethod]=useState('facial_document'),[expiresAt,setExpiresAt]=useState('');
 const [deliveryEmail,setDeliveryEmail]=useState(true),[deliveryWhatsapp,setDeliveryWhatsapp]=useState(true),[documensoToken,setDocumensoToken]=useState('');
 const [provider,setProvider]=useState<ProviderStatus>({documenso:{configured:false,reachable:false},biometrics:{configured:false,reachable:false}});
 const [bridgeOnline,setBridgeOnline]=useState(false),[bridgeChecked,setBridgeChecked]=useState(false),[certs,setCerts]=useState<LocalCert[]>([]),[certFingerprint,setCertFingerprint]=useState(''),[visibleSeal,setVisibleSeal]=useState(true);
 const selectedClient=useMemo(()=>clients.find(c=>c.id===selectedClientId)||null,[clients,selectedClientId]);
 const recipient=useMemo(()=>recipientMode==='client'?(selectedClient?{id:selectedClient.id,name:selectedClient.name,email:selectedClient.email||'',phone:selectedClient.whatsapp||selectedClient.phone||''}:null):(manualName.trim()?{id:null,name:manualName.trim(),email:manualEmail.trim(),phone:manualPhone.trim()}:null),[recipientMode,selectedClient,manualName,manualEmail,manualPhone]);
 const clientDocuments=useMemo(()=>documents.filter(d=>recipientMode==='client'&&d.client_id===selectedClientId&&!/(assinado|validado)/i.test(d.category||'')),[documents,selectedClientId,recipientMode]);
 const documentMap=useMemo(()=>new Map(documents.map(d=>[d.id,d])),[documents]);
 const dashboard=useMemo(()=>rows.map(r=>{const c=clients.find(i=>i.id===r.client_id);return{...r,clientName:c?.name||r.signer_name||'Destinatário',readyForIcp:isSigned(r.status)&&r.verification_status==='approved'&&Boolean(r.signed_document_id)&&!r.icp_validated_document_id,validated:Boolean(r.icp_validated_document_id)}}),[rows,clients]);
 const stats=useMemo(()=>({total:dashboard.length,draft:dashboard.filter(r=>(r.status||'').toLowerCase()==='draft').length,waiting:dashboard.filter(r=>isWaiting(r.status)).length,ready:dashboard.filter(r=>r.readyForIcp).length,validated:dashboard.filter(r=>r.validated).length}),[dashboard]);

 async function load(showFeedback=false){if(!supabase)return;setLoading(true);await supabase.functions.invoke('documenso-refresh-signatures',{body:{}}).catch(()=>null);const [{data,error},{data:docs,error:docsError},{data:cls,error:clientsError},providerResult]=await Promise.all([supabase.from('signature_requests').select('id,title,status,external_id,sent_at,signed_at,expires_at,document_id,client_id,signer_name,signer_phone,created_at,signed_document_id,signed_file_captured_at,icp_validated_document_id,icp_validated_at,verification_method,verification_status,delivery_email,delivery_whatsapp,batch_id,batch_order').order('created_at',{ascending:false}),supabase.from('documents').select('id,name,file_path,client_id,process_id,mime_type,category,created_at').eq('mime_type','application/pdf').order('created_at',{ascending:false}).limit(2000),supabase.from('clients').select('id,name,email,phone,whatsapp,cpf_cnpj').order('name').limit(2000),supabase.functions.invoke('signature-provider-status',{body:{}})]);if(error)setNotice(error.message);else if(docsError)setNotice(docsError.message);else if(clientsError)setNotice(clientsError.message);else if(showFeedback)setNotice('Painel atualizado.');setRows((data||[]) as RequestRow[]);setDocuments((docs||[]) as DocumentRow[]);setClients((cls||[]) as ClientRow[]);if(!providerResult.error&&providerResult.data)setProvider(providerResult.data as ProviderStatus);setLoading(false)}
 useEffect(()=>{load();checkBridge(false);const timer=window.setInterval(()=>load(false),90000);return()=>window.clearInterval(timer)},[]);
 async function configureDocumenso(){if(!supabase||!documensoToken.trim())return;setBusy('documenso');setNotice('Conectando o Documenso Cloud...');try{const{data,error}=await supabase.functions.invoke('documenso-configure',{body:{token:documensoToken.trim()}});if(error)throw error;if((data as any)?.error)throw new Error((data as any).error);setDocumensoToken('');setNotice((data as any)?.message||'Documenso Cloud conectado.');await load(false)}catch(e:any){setNotice(e?.message||'Não foi possível conectar o Documenso Cloud.')}finally{setBusy(null)}}
 async function checkBridge(showFeedback=true){setBusy('bridge');setBridgeChecked(false);try{const response=await fetch(`${BRIDGE}/health`,{signal:AbortSignal.timeout(3000)});if(!response.ok)throw new Error();const health=await response.json().catch(()=>null);if(!health?.ok)throw new Error();setBridgeOnline(true);if(showFeedback)setNotice(`Ponte ICP-Brasil conectada${health?.version?` • versão ${health.version}`:''}.`)}catch{setBridgeOnline(false);setCerts([]);setCertFingerprint('');if(showFeedback)setNotice('Ponte ICP-Brasil não encontrada. Abra o programa da ponte e tente novamente.')}finally{setBridgeChecked(true);setBusy(null)}}
 async function loadCertificates(){if(!bridgeOnline){setNotice('A ponte está offline. Clique em Verificar ponte primeiro.');return}setBusy('certificates');setNotice('Aguardando o PIN do token A3 no seu computador...');try{const r=await fetch(`${BRIDGE}/certificates`,{signal:AbortSignal.timeout(60000)}),raw=await r.text();let json:any={};try{json=raw?JSON.parse(raw):{}}catch{}if(!r.ok)throw new Error(json?.error||raw||'Não foi possível acessar o certificado A3.');const list=(Array.isArray(json)?json:(json.certificates||[])) as LocalCert[];setCerts(list);setCertFingerprint(c=>c&&list.some(i=>i.fingerprint===c)?c:(list[0]?.fingerprint||''));setNotice(list.length?'Certificado A3 reconhecido e pronto.':'Nenhum certificado foi encontrado no token.')}catch(e:any){setNotice(e?.name==='TimeoutError'?'Tempo esgotado aguardando o PIN.':(e?.message||'Não foi possível carregar o certificado A3.'))}finally{setBusy(null)}}
 async function createRequest(document:DocumentRow,orgId:string,userId:string,batchId:string,batchOrder:number){if(!supabase||!recipient)throw new Error('Destinatário não informado.');const{data:request,error}=await supabase.from('signature_requests').insert({org_id:orgId,client_id:recipient.id,process_id:document.process_id||null,document_id:document.id,title:document.name,signer_name:recipient.name,signer_email:recipient.email||null,signer_phone:recipient.phone||null,provider:'documenso',status:'draft',verification_method:verificationMethod,verification_status:'pending',verification_provider:'lexoffice_native',delivery_email:deliveryEmail,delivery_whatsapp:deliveryWhatsapp,expires_at:expiresAt?new Date(`${expiresAt}T23:59:59`).toISOString():null,created_by:userId,batch_id:batchId,batch_order:batchOrder}).select('id').single();if(error||!request)throw error||new Error('Não foi possível criar a solicitação.');const{error:signerError}=await supabase.from('signature_signers').insert({org_id:orgId,signature_request_id:request.id,name:recipient.name,email:recipient.email||null,phone:recipient.phone||null,signing_order:1,status:'pending'});if(signerError){await supabase.from('signature_requests').delete().eq('id',request.id);throw signerError}return request.id as string}
 async function prepareAndSend(){if(!supabase||!recipient){setNotice('Selecione um cliente ou digite o destinatário.');return}const chosen=existingDocumentIds.map(id=>documentMap.get(id)).filter(Boolean) as DocumentRow[];if(!localFiles.length&&!chosen.length){setNotice('Adicione pelo menos um PDF.');return}if(!recipient.email){setNotice('Informe o e-mail do destinatário. Ele é necessário para a assinatura.');return}if(deliveryWhatsapp&&!recipient.phone){setNotice('Informe o WhatsApp do destinatário ou desmarque o envio por WhatsApp.');return}if(!provider.biometrics.reachable){setNotice('A verificação facial está indisponível. O envio foi bloqueado por segurança.');return}if(!provider.documenso.reachable){setNotice('Conecte o Documenso Cloud antes de enviar.');return}setBusy('batch');setNotice('Preparando um link único para todos os documentos...');try{const{data:{user}}=await supabase.auth.getUser();if(!user)throw new Error('Sessão expirada.');const{data:profile}=await supabase.from('profiles').select('org_id').eq('id',user.id).maybeSingle();if(!profile?.org_id)throw new Error('Organização não identificada.');const prepared=[...chosen];for(const file of localFiles){if(file.type&&file.type!=='application/pdf')throw new Error(`${file.name} não é um PDF.`);const owner=recipient.id||`manual-${crypto.randomUUID()}`;const path=`${profile.org_id}/signatures/${owner}/originais/${Date.now()}-${crypto.randomUUID()}-${safeName(file.name)}`;const{error:uploadError}=await supabase.storage.from('lexoffice-documents').upload(path,file,{contentType:'application/pdf',upsert:false});if(uploadError)throw uploadError;const{data:doc,error:docError}=await supabase.from('documents').insert({org_id:profile.org_id,client_id:recipient.id,name:file.name,file_path:path,mime_type:'application/pdf',size_bytes:file.size,category:'Documento para assinatura',notes:`Destinatário: ${recipient.name}`,uploaded_by:user.id}).select('id,name,file_path,client_id,process_id,mime_type,category,created_at').single();if(docError||!doc)throw docError||new Error('Não foi possível registrar o PDF.');prepared.push(doc as DocumentRow)}const batchId=crypto.randomUUID();const ids:string[]=[];for(let i=0;i<prepared.length;i++)ids.push(await createRequest(prepared[i],profile.org_id,user.id,batchId,i+1));const{data,error}=await supabase.functions.invoke('documenso-send-signature',{body:{signature_request_ids:ids,action:'send'}});if(error)throw error;if((data as any)?.error)throw new Error((data as any).error);const failed=((data as any)?.results||[]).filter((i:any)=>!i.ok);if(failed.length)throw new Error(failed.map((i:any)=>i.error).join(' • '));setLocalFiles([]);setExistingDocumentIds([]);setNotice(ids.length>1?`Lote com ${ids.length} documentos preparado. O destinatário receberá UM ÚNICO LINK, fará a verificação uma vez e assinará todos os documentos no mesmo fluxo.`:'Documento enviado para verificação de identidade e assinatura.');await load(false)}catch(e:any){setNotice(e?.message||'Não foi possível enviar os documentos.')}finally{setBusy(null)}}
 async function remind(row:RequestRow){if(!supabase)return;setBusy(`remind:${row.id}`);const{data,error}=await supabase.functions.invoke('documenso-send-signature',{body:{signature_request_ids:[row.id],action:'remind'}});setNotice(error?error.message:(((data as any)?.results||[]).find((i:any)=>!i.ok)?.error||`Instruções e link único reenviados para ${row.signer_name||'o destinatário'}.`));setBusy(null)}
 async function removeRequest(row:RequestRow){if(!supabase)return;if(row.icp_validated_document_id||isSigned(row.status)){setNotice('Documento já assinado/finalizado não pode ser excluído por esta tela.');return}if(!window.confirm(`Excluir “${row.title||'Documento'}” da lista de assinaturas? O PDF original permanecerá na biblioteca.`))return;setBusy(`delete:${row.id}`);try{const{data,error}=await supabase.functions.invoke('documenso-send-signature',{body:{signature_request_ids:[row.id],action:'delete'}});if(error)throw error;const failed=((data as any)?.results||[]).find((i:any)=>!i.ok);if(failed)throw new Error(failed.error);setNotice('Documento excluído da lista de assinaturas.');await load(false)}catch(e:any){setNotice(e?.message||'Não foi possível excluir o documento.')}finally{setBusy(null)}}
 async function signIcpForRequest(row:RequestRow){if(!supabase||!row.signed_document_id)return;if(row.verification_status!=='approved'){setNotice('A validação ICP só é liberada após a verificação de identidade aprovada.');return}const signedDoc=documentMap.get(row.signed_document_id);if(!signedDoc?.file_path){setNotice('O PDF assinado ainda está sendo recebido.');return}if(!bridgeOnline){setNotice('A ponte ICP-Brasil está offline.');return}if(!certFingerprint){await loadCertificates();return}setBusy(`icp:${row.id}`);setNotice('Validando o documento com seu certificado ICP-Brasil...');try{const{data:pdf,error}=await supabase.storage.from('lexoffice-documents').download(signedDoc.file_path);if(error||!pdf)throw error||new Error('PDF assinado não encontrado.');const form=new FormData();form.append('file',pdf,`${row.title||'documento'}.pdf`);form.append('certificateFingerprint',certFingerprint);form.append('visibleSeal',String(visibleSeal));form.append('sealText','Validado com certificado ICP-Brasil');const response=await fetch(`${BRIDGE}/sign/pades`,{method:'POST',body:form});if(!response.ok)throw new Error((await response.text())||'A validação ICP-Brasil não foi concluída.');const blob=await response.blob();if(!blob.size)throw new Error('O arquivo validado retornou vazio.');const{data:{user}}=await supabase.auth.getUser();const{data:profile}=user?await supabase.from('profiles').select('org_id').eq('id',user.id).maybeSingle():({data:null} as any);if(!profile?.org_id)throw new Error('Organização não identificada.');const path=`${profile.org_id}/signatures/${row.id}/final/${Date.now()}-${safeName(row.title||'documento')}-validado-icpbr.pdf`;const{error:uploadError}=await supabase.storage.from('lexoffice-documents').upload(path,blob,{contentType:'application/pdf'});if(uploadError)throw uploadError;const cert=certs.find(i=>i.fingerprint===certFingerprint);const{data:validatedDoc,error:insertError}=await supabase.from('documents').insert({org_id:profile.org_id,client_id:row.client_id||null,process_id:signedDoc.process_id||null,name:`${(row.title||'Documento').replace(/\.pdf$/i,'')} — Validado ICP-Brasil.pdf`,file_path:path,mime_type:'application/pdf',size_bytes:blob.size,category:'Documento validado ICP-Brasil',notes:`PAdES ICP-Brasil • ${cert?.subject||'certificado A3'} • ${new Date().toLocaleString('pt-BR')}`,uploaded_by:user?.id||null}).select('id').single();if(insertError||!validatedDoc)throw insertError||new Error('Não foi possível registrar o documento final.');const now=new Date().toISOString();const{error:requestError}=await supabase.from('signature_requests').update({icp_validated_document_id:validatedDoc.id,icp_validated_at:now,updated_at:now}).eq('id',row.id);if(requestError)throw requestError;setNotice('Documento validado com ICP-Brasil e pronto para download.');await load(false)}catch(e:any){setNotice(e?.message||'Não foi possível validar o documento.')}finally{setBusy(null)}}
 async function downloadFinal(row:RequestRow){if(!supabase||!row.icp_validated_document_id)return;const doc=documentMap.get(row.icp_validated_document_id);if(!doc?.file_path)return setNotice('Documento final não encontrado.');setBusy(`download:${row.id}`);try{const{data,error}=await supabase.storage.from('lexoffice-documents').download(doc.file_path);if(error||!data)throw error||new Error();const url=URL.createObjectURL(data),a=document.createElement('a');a.href=url;a.download=doc.name||'documento-validado-icp-brasil.pdf';document.body.appendChild(a);a.click();a.remove();URL.revokeObjectURL(url)}catch(e:any){setNotice(e?.message||'Não foi possível baixar o documento.')}finally{setBusy(null)}}
 const statusBadge=(row:RequestRow)=>{const s=(row.status||'draft').toLowerCase();if(row.icp_validated_document_id)return <span className="sig-badge validated"><CheckCircle2 size={12}/> Validado ICP</span>;if(s==='verification_pending')return <span className="sig-badge waiting"><Fingerprint size={12}/> Aguardando verificação</span>;if(s==='signed'&&row.signed_document_id)return <span className="sig-badge ready"><FileCheck2 size={12}/> Assinado</span>;if(s==='signed')return <span className="sig-badge signed"><RefreshCw size={12}/> Recebendo PDF</span>;if(s==='viewed')return <span className="sig-badge viewed"><Activity size={12}/> Visualizado</span>;if(s==='sent')return <span className="sig-badge waiting"><Bell size={12}/> Aguardando assinatura</span>;if(['declined','cancelled','expired'].includes(s))return <span className="sig-badge error"><AlertTriangle size={12}/> {s}</span>;return <span className="sig-badge draft"><Activity size={12}/> Aguardando envio</span>};

 return <><div className="page-title signature-page-title"><div><h1>Assinaturas</h1><p>Verificação de identidade, assinatura em lote com link único e validação final ICP-Brasil.</p></div><div className="signature-top-actions"><span className={`provider-pill ${provider.documenso.reachable?'ok':'off'}`}><Send size={13}/> Documenso Cloud {provider.documenso.reachable?'online':'não conectado'}</span><span className={`provider-pill ${bridgeOnline?'ok':'off'}`}>{bridgeOnline?<Wifi size={13}/>:<WifiOff size={13}/>} A3 {bridgeOnline?'conectado':'offline'}</span><button className="sig-btn ghost" onClick={()=>checkBridge(true)} disabled={busy==='bridge'}><RefreshCw size={14}/> Verificar A3</button></div></div>
 {notice&&<div className="integration-notice signature-notice">{notice}</div>}
 {!provider.documenso.reachable&&<section className="signature-workbench" style={{marginBottom:16}}><div className="signature-section-head"><div><span className="eyebrow">CONEXÃO DE ASSINATURA</span><h2>Conectar Documenso Cloud</h2></div></div><div className="signature-sendbar"><div style={{flex:1,minWidth:280}}><input type="password" value={documensoToken} onChange={e=>setDocumensoToken(e.target.value)} placeholder="API Token do Documenso" style={{width:'100%'}}/></div><button className="sig-btn primary" onClick={configureDocumenso} disabled={busy==='documenso'||!documensoToken.trim()}><KeyRound size={15}/>Conectar Cloud</button></div></section>}
 <div className="signature-kpis compact"><div><FileText size={17}/><span>Total</span><strong>{stats.total}</strong></div><div className="pending"><Activity size={17}/><span>Aguardando envio</span><strong>{stats.draft}</strong></div><div className="waiting"><Bell size={17}/><span>Em andamento</span><strong>{stats.waiting}</strong></div><div><FileCheck2 size={17}/><span>Prontos para ICP</span><strong>{stats.ready}</strong></div><div className="done"><ShieldCheck size={17}/><span>Finalizados</span><strong>{stats.validated}</strong></div></div>
 <section className="signature-workbench"><div className="signature-section-head"><div><span className="eyebrow">NOVA SOLICITAÇÃO</span><h2>Enviar documento</h2></div><span className="step-hint">1 Destinatário → 2 PDFs → 3 Verificação única → 4 Assinar todos</span></div>
 <div style={{display:'flex',gap:8,marginBottom:14}}><button type="button" className={`sig-btn ${recipientMode==='client'?'primary':'ghost'}`} onClick={()=>{setRecipientMode('client');setExistingDocumentIds([])}}>Cliente cadastrado</button><button type="button" className={`sig-btn ${recipientMode==='manual'?'primary':'ghost'}`} onClick={()=>{setRecipientMode('manual');setSelectedClientId('');setExistingDocumentIds([])}}>Digitar destinatário</button></div>
 <div className="signature-compose-grid">
 {recipientMode==='client'?<label className="sig-field wide"><span><UserRound size={14}/> Cliente cadastrado</span><select value={selectedClientId} onChange={e=>{setSelectedClientId(e.target.value);setExistingDocumentIds([])}}><option value="">Selecione o cliente</option>{clients.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select>{selectedClient&&<small>{selectedClient.email||'Sem e-mail'} • {selectedClient.whatsapp||selectedClient.phone||'Sem WhatsApp'}</small>}</label>:<><label className="sig-field"><span><UserRound size={14}/> Nome do destinatário</span><input value={manualName} onChange={e=>setManualName(e.target.value)} placeholder="Nome completo"/></label><label className="sig-field"><span>E-mail</span><input type="email" value={manualEmail} onChange={e=>setManualEmail(e.target.value)} placeholder="email@exemplo.com"/></label><label className="sig-field"><span>WhatsApp</span><input value={manualPhone} onChange={e=>setManualPhone(e.target.value)} placeholder="(31) 99999-9999"/></label></>}
 <label className="sig-field upload-field"><span><UploadCloud size={14}/> PDFs</span><input type="file" accept="application/pdf,.pdf" multiple onChange={e=>setLocalFiles(Array.from(e.target.files||[]))}/><small>{localFiles.length?`${localFiles.length} PDF${localFiles.length>1?'s':''} selecionado${localFiles.length>1?'s':''}`:'Escolha um ou vários arquivos — todos irão no mesmo link'}</small></label>
 <label className="sig-field"><span><Fingerprint size={14}/> Verificação de identidade</span><select value={verificationMethod} onChange={e=>setVerificationMethod(e.target.value)}>{verificationOptions.map(o=><option key={o.value} value={o.value}>{o.label}</option>)}</select><small>{verificationOptions.find(i=>i.value===verificationMethod)?.detail}</small></label><label className="sig-field"><span>Prazo</span><input type="date" value={expiresAt} onChange={e=>setExpiresAt(e.target.value)}/><small>Opcional</small></label></div>
 {recipientMode==='client'&&selectedClientId&&clientDocuments.length>0&&<details className="library-picker"><summary>Usar também PDFs já salvos na pasta do cliente</summary><div className="library-items">{clientDocuments.slice(0,30).map(doc=><label key={doc.id}><input type="checkbox" checked={existingDocumentIds.includes(doc.id)} onChange={e=>setExistingDocumentIds(cur=>e.target.checked?[...cur,doc.id]:cur.filter(id=>id!==doc.id))}/><span>{doc.name}</span></label>)}</div></details>}
 <div className="signature-sendbar"><div className="delivery-options"><label><input type="checkbox" checked={deliveryEmail} onChange={e=>setDeliveryEmail(e.target.checked)}/> E-mail</label><label><input type="checkbox" checked={deliveryWhatsapp} onChange={e=>setDeliveryWhatsapp(e.target.checked)}/> WhatsApp</label><span>{localFiles.length+existingDocumentIds.length} documento{localFiles.length+existingDocumentIds.length===1?'':'s'} • 1 link único</span></div><button className="sig-btn primary" onClick={prepareAndSend} disabled={busy==='batch'||!recipient||(!localFiles.length&&!existingDocumentIds.length)||!provider.documenso.reachable}><Send size={16}/>{busy==='batch'?'Preparando lote...':'Enviar link único'}</button></div>
 {!provider.biometrics.reachable&&<div className="provider-warning"><AlertTriangle size={15}/> A verificação facial está indisponível; o envio fica bloqueado até o serviço estar pronto.</div>}</section>
 <section className="signature-table-panel"><div className="signature-section-head table-head"><div><span className="eyebrow">ACOMPANHAMENTO</span><h2>Documentos enviados</h2></div><div className="table-actions">{bridgeOnline&&<button className="sig-btn ghost" onClick={loadCertificates} disabled={busy==='certificates'}><KeyRound size={14}/>{certFingerprint?'A3 carregado':busy==='certificates'?'Aguardando PIN...':'Carregar A3'}</button>}<label className="seal-toggle"><input type="checkbox" checked={visibleSeal} onChange={e=>setVisibleSeal(e.target.checked)}/> selo visual</label><button className="sig-btn ghost" onClick={()=>load(true)} disabled={loading}><RefreshCw size={14}/> Atualizar</button></div></div><div className="signature-table-wrap"><table className="signature-table"><thead><tr><th>Destinatário</th><th>Documento</th><th>Verificação</th><th>Status</th><th>ICP-Brasil</th><th>Ações</th></tr></thead><tbody>{loading?<tr><td colSpan={6} className="empty-cell">Carregando...</td></tr>:dashboard.length===0?<tr><td colSpan={6} className="empty-cell">Nenhum documento em acompanhamento.</td></tr>:dashboard.map(row=><tr key={row.id}><td><strong>{row.clientName}</strong></td><td><span className="doc-title">{row.title||'Documento'}</span><small>{row.batch_id?`Lote • documento ${row.batch_order||1}`:(row.sent_at?`Enviado ${new Date(row.sent_at).toLocaleDateString('pt-BR')}`:'Ainda não enviado')}</small></td><td><span className="verification-chip">{verificationLabel(row.verification_method)}</span></td><td>{statusBadge(row)}</td><td>{row.icp_validated_document_id?<span className="sig-badge validated"><ShieldCheck size={12}/> Selo aplicado</span>:row.readyForIcp?<span className="sig-badge ready">Pronto para validar</span>:<span className="muted-cell">—</span>}</td><td><div className="row-actions">{isWaiting(row.status)&&<button className="sig-btn mini" onClick={()=>remind(row)} disabled={busy===`remind:${row.id}`}><Bell size={13}/> Lembrar</button>}{isSigned(row.status)&&row.signed_document_id&&row.verification_status==='approved'&&!row.icp_validated_document_id&&<button className="sig-btn mini primary" onClick={()=>signIcpForRequest(row)} disabled={busy===`icp:${row.id}`||!bridgeOnline}><ShieldCheck size={13}/>{busy===`icp:${row.id}`?'Validando...':'Validar ICP'}</button>}{row.icp_validated_document_id&&<button className="sig-btn mini" onClick={()=>downloadFinal(row)}><Download size={13}/> Baixar PDF</button>}{!isSigned(row.status)&&!row.icp_validated_document_id&&<button className="sig-btn mini" onClick={()=>removeRequest(row)} disabled={busy===`delete:${row.id}`}><Trash2 size={13}/>{busy===`delete:${row.id}`?'Excluindo...':'Excluir'}</button>}</div></td></tr>)}</tbody></table></div></section></>;
}
