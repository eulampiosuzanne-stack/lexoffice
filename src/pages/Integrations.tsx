import { useEffect,useState } from 'react';
import { Plug,MessageCircle,CalendarDays,PenTool,Gavel,Bot,CheckCircle2,RefreshCw,ExternalLink } from 'lucide-react';
import { supabase } from '../lib/supabase';
import './integrations.css';
type Conn={provider?:string;status?:string;name?:string;updated_at?:string};
const catalog=[{key:'whatsapp',name:'WhatsApp',desc:'Atendimento, mensagens e automações por QR Code.',icon:MessageCircle,path:'/whatsapp'},{key:'google',name:'Google Agenda',desc:'OAuth e sincronização de compromissos do escritório.',icon:CalendarDays,path:'/agenda'},{key:'zapsign',name:'ZapSign',desc:'Envio e acompanhamento de assinaturas eletrônicas.',icon:PenTool,path:'/assinaturas'},{key:'tribunal',name:'Tribunais',desc:'Conexões processuais, sincronização e monitoramento.',icon:Gavel,path:'/processos?regra=tribunals'},{key:'openai',name:'Inteligência Artificial',desc:'Modelos e agentes especializados do LEXOFFICE.',icon:Bot,path:'/agentes-ia'}];
export default function Integrations(){const [connections,setConnections]=useState<Conn[]>([]);const [loading,setLoading]=useState(true);useEffect(()=>{if(!supabase){setLoading(false);return}(async()=>{const {data}=await supabase.from('integration_connections').select('provider,status,name,updated_at').order('updated_at',{ascending:false});setConnections(data||[]);setLoading(false)})()},[]);function state(key:string){const c=connections.find(x=>(x.provider||'').toLowerCase().includes(key));return c?.status||null}return <div><div className="page-head"><div><h1>Central de Integrações</h1><p>Conexões que alimentam agentes, automações e módulos do ecossistema.</p></div><span className="integration-pill"><Plug size={15}/> ECOSSISTEMA CONECTADO</span></div><div className="connections-grid">{catalog.map(item=>{const Icon=item.icon;const s=state(item.key);const active=s&&['active','connected','online','ok'].includes(s.toLowerCase());return <a className="connection-card" href={item.path} key={item.key}><div className="connection-top"><div className="integration-icon"><Icon/></div><span className={active?'conn-status on':'conn-status'}>{active?<CheckCircle2 size={13}/>:<RefreshCw size={13}/>} {loading?'Verificando':active?'Conectado':s||'Configurar'}</span></div><h2>{item.name}</h2><p>{item.desc}</p><div className="connection-action">Abrir configuração <ExternalLink size={14}/></div></a>})}</div><div className="module-box"><h3>Arquitetura integrada</h3><p>As conexões são reutilizadas pelos módulos e pelos agentes de IA. Nenhuma credencial sensível é exibida nesta central.</p></div></div>}
