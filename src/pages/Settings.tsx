import { useEffect,useState } from 'react';
import { Save } from 'lucide-react';
import { supabase } from '../lib/supabase';

const empty={organization_name:'',trade_name:'',document_number:'',email:'',phone:'',whatsapp:'',website:'',timezone:'America/Sao_Paulo',locale:'pt-BR'};
export default function Settings(){const [form,setForm]=useState<any>(empty),[orgId,setOrgId]=useState(''),[id,setId]=useState(''),[saving,setSaving]=useState(false),[notice,setNotice]=useState('');useEffect(()=>{(async()=>{if(!supabase)return;const {data:{user}}=await supabase.auth.getUser();if(!user)return;const {data:p}=await supabase.from('profiles').select('org_id').eq('id',user.id).maybeSingle();if(!p?.org_id)return;setOrgId(p.org_id);const {data:s}=await supabase.from('organization_settings').select('id,organization_name,trade_name,document_number,email,phone,whatsapp,website,timezone,locale').eq('org_id',p.org_id).maybeSingle();if(s){setId(s.id);setForm({...empty,...s})}})()},[]);function change(k:string,v:string){setForm((x:any)=>({...x,[k]:v}))}async function save(){if(!supabase||!orgId)return;setSaving(true);setNotice('');const payload={org_id:orgId,organization_name:form.organization_name||null,trade_name:form.trade_name||null,document_number:form.document_number||null,email:form.email||null,phone:form.phone||null,whatsapp:form.whatsapp||null,website:form.website||null,timezone:form.timezone||'America/Sao_Paulo',locale:form.locale||'pt-BR',updated_at:new Date().toISOString()};const q=id?supabase.from('organization_settings').update(payload).eq('id',id):supabase.from('organization_settings').insert(payload).select('id').single();const {data,error}=await q as any;if(!error&&data?.id)setId(data.id);setNotice(error?'Não foi possível salvar as configurações.':'Configurações salvas com sucesso.');setSaving(false)}return <div className="module"><div className="page-head"><div><h1>Configurações</h1><p>Dados institucionais e preferências do escritório</p></div><button className="primary" onClick={save} disabled={saving}><Save size={15}/>{saving?'Salvando...':'Salvar'}</button></div><div className="module-box settings-panel"><div className="form-grid"><label>Razão social<input value={form.organization_name} onChange={e=>change('organization_name',e.target.value)}/></label><label>Nome fantasia<input value={form.trade_name} onChange={e=>change('trade_name',e.target.value)}/></label><label>CPF/CNPJ<input value={form.document_number} onChange={e=>change('document_number',e.target.value)}/></label><label>E-mail<input type="email" value={form.email} onChange={e=>change('email',e.target.value)}/></label><label>Telefone<input value={form.phone} onChange={e=>change('phone',e.target.value)}/></label><label>WhatsApp<input value={form.whatsapp} onChange={e=>change('whatsapp',e.target.value)}/></label><label>Website<input value={form.website} onChange={e=>change('website',e.target.value)}/></label><label>Fuso horário<select value={form.timezone} onChange={e=>change('timezone',e.target.value)}><option value="America/Sao_Paulo">Brasília / São Paulo</option><option value="America/Manaus">Manaus</option><option value="America/Rio_Branco">Rio Branco</option></select></label><label>Idioma<select value={form.locale} onChange={e=>change('locale',e.target.value)}><option value="pt-BR">Português (Brasil)</option></select></label></div>{notice&&<div className="login-notice">{notice}</div>}</div></div>}
