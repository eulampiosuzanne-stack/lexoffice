import {useEffect,useState} from 'react';
import {Landmark,Plus,RefreshCw,ShieldCheck,KeyRound,Search,UserCheck,LockKeyhole,CheckCircle2,AlertTriangle,Play} from 'lucide-react';
import {supabase} from '../lib/supabase';
import './integrations.css';

type Conn={id:string;tribunal_code:string;tribunal_name:string;judicial_system:string|null;oab_number:string|null;oab_uf:string|null;status:string;requires_2fa:boolean;last_sync_at:string|null;error_message:string|null};
const ufs=['AC','AL','AP','AM','BA','CE','DF','ES','GO','MA','MT','MS','MG','PA','PB','PR','PE','PI','RJ','RN','RS','RO','RR','SC','SP','SE','TO'];
const national=[['STF','Supremo Tribunal Federal'],['STJ','Superior Tribunal de Justiça'],['TST','Tribunal Superior do Trabalho'],['TSE','Tribunal Superior Eleitoral'],['STM','Superior Tribunal Militar'],...[1,2,3,4,5,6].map(n=>[`TRF${n}`,`Tribunal Regional Federal da ${n}ª Região`]),...ufs.map(uf=>[`TJ${uf}`,`Tribunal de Justiça de ${uf}`]),...Array.from({length:24},(_,i)=>[`TRT${i+1}`,`Tribunal Regional do Trabalho da ${i+1}ª Região`]),...ufs.map(uf=>[`TRE${uf}`,`Tribunal Regional Eleitoral de ${uf}`])] as [string,string][];
const systems=['PJe','eproc','Projudi','SAJ / e-SAJ','SEEU','Creta','Apolo','DataJud','Sistema próprio','Outro'];
const nameOf=(c:string)=>national.find(x=>x[0]===c)?.[1]||c;
async function edgeErrorMessage(error:any){const ctx=error?.context;if(ctx&&typeof ctx.clone==='function'){try{const data=await ctx.clone().json();const msg=data?.error||data?.message||data?.msg;if(msg)return String(msg)}catch{}const status=Number(ctx?.status||0);if(status===401)return 'Sua sessão expirou ou não é mais válida. Entre novamente no LEXOFFICE.';if(status===403)return 'Seu usuário não tem permissão para executar esta operação.';if(status===404)return 'O serviço solicitado não foi encontrado.';if(status>=500)return 'O serviço de tribunais encontrou um erro interno. O detalhe foi registrado no servidor.';}const msg=String(error?.message||'').trim();if(msg&&!/Edge Function returned a non-2xx status code/i.test(msg))return msg;return 'Não foi possível concluir a operação com o tribunal.';}
async function fn(name:string,body:any){if(!supabase)throw new Error('Backend indisponível.');const r=await supabase.functions.invoke(name,{body});if(r.data?.error)throw new Error(r.data.error);if(r.error)throw new Error(await edgeErrorMessage(r.error));return r.data||{}}
export default function Tribunals(){
 const[tab,setTab]=useState('access'),[connections,setConnections]=useState<Conn[]>([]),[busy,setBusy]=useState(false),[notice,setNotice]=useState(''),[error,setError]=useState(false),[worker,setWorker]=useState<any>({jobs:[],interventions:[]});
 const[cred,setCred]=useState({tribunal_code:'TJMG',judicial_system:'PJe',oab_number:'',oab_uf:'MG',login:'',password:'',two_factor_code:''});
 const[oab,setOab]=useState({tribunal_code:'TJMG',judicial_system:'PJe',oab_number:'',oab_uf:'MG'});const[manual,setManual]=useState({cnj_number:'',tribunal_code:'TJMG',judicial_system:'PJe',subject:''});
 async function load(){try{const[a,b]=await Promise.all([fn('tribunal-connection-manager',{action:'list'}),fn('tribunal-worker-config',{action:'status'}).catch(()=>({jobs:[],interventions:[]}))]);setConnections(a.connections||[]);setWorker(b)}catch{}}
 useEffect(()=>{load();const id=setInterval(load,45000);return()=>clearInterval(id)},[]);
 async function run(cb:()=>Promise<any>){setBusy(true);setNotice('');try{const r=await cb();setError(false);setNotice(r.message||'Salvo com sucesso.');await load();return r}catch(e:any){setError(true);setNotice(e?.message||'Não foi possível salvar.')}finally{setBusy(false)}}
 async function saveAccess(){const code=cred.two_factor_code.trim();if(code&&code.length!==6){setError(true);setNotice('O código do Google Authenticator deve ter 6 dígitos.');return}const r=await run(()=>fn('tribunal-worker-config',{action:code?'verify_2fa':'save_credentials',...cred,tribunal_name:nameOf(cred.tribunal_code),requires_2fa:Boolean(code)}));if(r?.ok)setCred(v=>({...v,password:code?v.password:'',two_factor_code:''}))}
 async function saveOab(){await run(async()=>{await fn('tribunal-connection-manager',{action:'save',tribunal_code:oab.tribunal_code,tribunal_name:nameOf(oab.tribunal_code),judicial_system:oab.judicial_system,oab_number:oab.oab_number,oab_uf:oab.oab_uf,requires_2fa:true});return fn('tribunal-connection-manager',{action:'import_oab',tribunal_code:oab.tribunal_code,tribunal_name:nameOf(oab.tribunal_code),judicial_system:oab.judicial_system,oab_number:oab.oab_number,oab_uf:oab.oab_uf})})}
 async function sync(c:Conn){await run(()=>fn('tribunal-worker-config',{action:'enqueue',connection_id:c.id}))}async function addManual(){await run(()=>fn('tribunal-connection-manager',{action:'add_process',...manual,tribunal_name:nameOf(manual.tribunal_code)}));setManual(v=>({...v,cnj_number:'',subject:''}))}
 const tribunalSelect=(value:string,onChange:(v:string)=>void)=><select value={value} onChange={e=>onChange(e.target.value)}>{national.map(([c,n])=><option value={c} key={c}>{c} — {n}</option>)}</select>;const systemSelect=(value:string,onChange:(v:string)=>void)=><select value={value} onChange={e=>onChange(e.target.value)}>{systems.map(s=><option key={s}>{s}</option>)}</select>;
 return <div className="module"><div className="page-head"><div><h1>Tribunais e Processos</h1><p>Cadastre apenas os dados usados normalmente para acessar cada sistema judicial.</p></div><button className="integration-action" onClick={load}><RefreshCw size={16}/>Atualizar</button></div>{notice&&<div className={`integration-notice ${error?'error':''}`}>{error?<AlertTriangle size={17}/>:<CheckCircle2 size={17}/>} {notice}</div>}
 <div className="campaign-tabs process-tabs"><button className={tab==='access'?'active':''} onClick={()=>setTab('access')}><KeyRound size={16}/>Acessos e 2FA</button><button className={tab==='oab'?'active':''} onClick={()=>setTab('oab')}><UserCheck size={16}/>Importar por OAB</button><button className={tab==='monitor'?'active':''} onClick={()=>setTab('monitor')}><ShieldCheck size={16}/>Monitoramento</button><button className={tab==='manual'?'active':''} onClick={()=>setTab('manual')}><Plus size={16}/>Adicionar processo</button></div>
 {tab==='access'&&<div className="integration-panel"><h3><LockKeyhole size={18}/> Acesso ao tribunal</h3><p>Salve o acesso. Quando o tribunal pedir Google Authenticator, informe o código atual de 6 dígitos e clique em Validar código 2FA.</p><div className="integration-form"><label>Tribunal{tribunalSelect(cred.tribunal_code,v=>setCred({...cred,tribunal_code:v,oab_uf:v.startsWith('TJ')?v.slice(2):cred.oab_uf}))}</label><label>Sistema processual{systemSelect(cred.judicial_system,v=>setCred({...cred,judicial_system:v}))}</label><label>Número da OAB<input value={cred.oab_number} onChange={e=>setCred({...cred,oab_number:e.target.value})} placeholder="131880"/></label><label>UF da OAB<input value={cred.oab_uf} maxLength={2} onChange={e=>setCred({...cred,oab_uf:e.target.value.toUpperCase()})} placeholder="MG"/></label><label>Login / CPF / usuário<input value={cred.login} onChange={e=>setCred({...cred,login:e.target.value})} autoComplete="username"/></label><label>Senha<input type="password" value={cred.password} onChange={e=>setCred({...cred,password:e.target.value})} autoComplete="current-password"/></label><label>Código Authenticator<input inputMode="numeric" value={cred.two_factor_code} onChange={e=>setCred({...cred,two_factor_code:e.target.value.replace(/\D/g,'').slice(0,6)})} placeholder="000000"/><small>O código é usado somente para a validação atual e não é armazenado.</small></label><button className="integration-action" onClick={saveAccess} disabled={busy||!cred.tribunal_code||!cred.judicial_system||!cred.oab_number||!cred.login||!cred.password||(Boolean(cred.two_factor_code)&&cred.two_factor_code.length!==6)}><KeyRound size={16}/>{busy?'Processando...':cred.two_factor_code?'Validar código 2FA':'Salvar acesso'}</button></div></div>}
 {tab==='oab'&&<div className="integration-panel"><h3><UserCheck size={18}/> Importar processos pela OAB</h3><p>Aqui são necessários somente OAB, UF, tribunal e sistema processual.</p><div className="integration-form"><label>Tribunal{tribunalSelect(oab.tribunal_code,v=>setOab({...oab,tribunal_code:v,oab_uf:v.startsWith('TJ')?v.slice(2):oab.oab_uf}))}</label><label>Sistema processual{systemSelect(oab.judicial_system,v=>setOab({...oab,judicial_system:v}))}</label><label>OAB<input value={oab.oab_number} onChange={e=>setOab({...oab,oab_number:e.target.value})}/></label><label>UF<input value={oab.oab_uf} maxLength={2} onChange={e=>setOab({...oab,oab_uf:e.target.value.toUpperCase()})}/></label><button className="integration-action" onClick={saveOab} disabled={busy||!oab.oab_number}>Salvar e buscar processos</button></div></div>}
 {tab==='monitor'&&<div className="integration-panel"><h3><ShieldCheck size={18}/> Monitoramento</h3>{connections.length===0?<p>Nenhum acesso cadastrado ainda.</p>:<div className="signature-list">{connections.map(c=><div className="signature-row" key={c.id}><div><strong>{c.tribunal_code} · {c.tribunal_name}</strong><small>{c.judicial_system||'—'} · OAB {c.oab_number||'—'}/{c.oab_uf||'—'}</small><small>Última sincronização: {c.last_sync_at?new Date(c.last_sync_at).toLocaleString('pt-BR'):'ainda não realizada'}{c.error_message?` · ${c.error_message}`:''}</small></div><button className="secondary" onClick={()=>sync(c)}><Play size={14}/>Sincronizar</button></div>)}</div>}</div>}
 {tab==='manual'&&<div className="integration-panel"><h3><Landmark size={18}/> Adicionar processo manualmente</h3><div className="integration-form"><label>Tribunal{tribunalSelect(manual.tribunal_code,v=>setManual({...manual,tribunal_code:v}))}</label><label>Sistema processual{systemSelect(manual.judicial_system,v=>setManual({...manual,judicial_system:v}))}</label><label className="wide">Número CNJ<input value={manual.cnj_number} onChange={e=>setManual({...manual,cnj_number:e.target.value})} placeholder="0000000-00.0000.0.00.0000"/></label><label className="wide">Assunto<input value={manual.subject} onChange={e=>setManual({...manual,subject:e.target.value})}/></label><button className="integration-action" onClick={addManual} disabled={busy||!manual.cnj_number}><Plus size={15}/>Adicionar processo</button></div></div>}</div>