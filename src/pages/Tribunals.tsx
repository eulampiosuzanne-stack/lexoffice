import {useEffect,useMemo,useState} from 'react';
import {Landmark,Plus,RefreshCw,ShieldCheck,KeyRound,Search,Gavel,UserCheck,LockKeyhole,EyeOff} from 'lucide-react';
import {supabase} from '../lib/supabase';
import './integrations.css';

type Conn={id:string;tribunal_code:string;tribunal_name:string;judicial_system:string|null;oab_number:string|null;oab_uf:string|null;status:string;requires_2fa:boolean;last_authenticated_at:string|null;last_sync_at:string|null;error_message:string|null};
const ufs=['AC','AL','AP','AM','BA','CE','DF','ES','GO','MA','MT','MS','MG','PA','PB','PR','PE','PI','RJ','RN','RS','RO','RR','SC','SP','SE','TO'];
const national=[['STF','Supremo Tribunal Federal'],['STJ','Superior Tribunal de Justiça'],['TST','Tribunal Superior do Trabalho'],['TSE','Tribunal Superior Eleitoral'],['STM','Superior Tribunal Militar'],...[1,2,3,4,5,6].map(n=>[`TRF${n}`,`Tribunal Regional Federal da ${n}ª Região`]),...ufs.map(uf=>[`TJ${uf}`,`Tribunal de Justiça de ${uf}`]),...Array.from({length:24},(_,i)=>[`TRT${i+1}`,`Tribunal Regional do Trabalho da ${i+1}ª Região`]),...ufs.map(uf=>[`TRE${uf}`,`Tribunal Regional Eleitoral de ${uf}`])] as [string,string][];
const platforms=['PJe','eproc','Projudi','SAJ / e-SAJ','DataJud','SEEU','Creta','Apolo','Sistema próprio','Outro'];
const emptyConn={tribunal_code:'TJMG',tribunal_name:'Tribunal de Justiça de MG',judicial_system:'PJe',oab_number:'',oab_uf:'MG',requires_2fa:true};
const emptyProcess={client_id:'',cnj_number:'',internal_number:'',opposing_party:'',tribunal_code:'TJMG',tribunal_name:'Tribunal de Justiça de MG',judicial_system:'PJe',court:'',comarca:'',vara:'',area:'',subject:'',class_name:'',is_confidential:false,confidentiality_level:'1'};
export default function Tribunals(){
 const[tab,setTab]=useState('connections'),[connections,setConnections]=useState<Conn[]>([]),[clients,setClients]=useState<any[]>([]),[form,setForm]=useState({...emptyConn}),[cred,setCred]=useState({tribunal_code:'TJMG',judicial_system:'PJe',login:'',password:''}),[two,setTwo]=useState({tribunal_code:'TJMG',judicial_system:'PJe',code:''}),[proc,setProc]=useState({...emptyProcess}),[busy,setBusy]=useState(false),[notice,setNotice]=useState(''),[q,setQ]=useState('');
 async function invoke(body:any){if(!supabase)throw new Error('Supabase indisponível.');const r=await supabase.functions.invoke('tribunal-connection-manager',{body});if(r.error)throw new Error(r.data?.error||r.error.message||'Falha na integração com tribunais.');if(r.data?.error)throw new Error(r.data.error);return r.data}
 async function load(){if(!supabase)return;setBusy(true);try{const[r,c]=await Promise.all([invoke({action:'list'}),supabase.from('clients').select('id,name,cpf_cnpj').order('name').limit(2000)]);setConnections(r.connections||[]);setClients(c.data||[]);setNotice('')}catch(e:any){setNotice(e?.message||'Falha ao carregar tribunais.')}finally{setBusy(false)}}
 useEffect(()=>{load();const id=setInterval(load,45000);return()=>clearInterval(id)},[]);
 const catalog=useMemo(()=>{const s=q.trim().toLowerCase();return s?national.filter(([c,n])=>(c+' '+n).toLowerCase().includes(s)):national},[q]);
 function chooseTribunal(code:string,name:string){const found=connections.find(c=>c.tribunal_code===code);const platform=found?.judicial_system||form.judicial_system||'PJe';setForm({...form,tribunal_code:code,tribunal_name:name,judicial_system:platform,oab_uf:code.startsWith('TJ')?code.slice(2):form.oab_uf});setCred({...cred,tribunal_code:code,judicial_system:platform});setTwo({...two,tribunal_code:code,judicial_system:platform})}
 function syncPlatform(value:string){setForm({...form,judicial_system:value});setCred({...cred,tribunal_code:form.tribunal_code,judicial_system:value});setTwo({...two,tribunal_code:form.tribunal_code,judicial_system:value})}
 async function run(fn:()=>Promise<any>,fallback:string){setBusy(true);setNotice('');try{const r=await fn();setNotice(r.message||'Operação concluída.');return r}catch(e:any){setNotice(e?.message||fallback)}finally{setBusy(false)}}
 async function saveConn(){const r=await run(()=>invoke({action:'save',...form}),'Falha ao salvar tribunal.');if(r)await load()}
 async function saveCred(){const r=await run(()=>invoke({action:'save_credentials',...cred}),'Falha ao salvar credenciais.');if(r){setCred({...cred,password:''});await load()}}
 async function submit2fa(){const r=await run(()=>invoke({action:'submit_2fa',...two}),'Falha no 2FA.');if(r)setTwo({...two,code:''})}
 async function addProcess(){const r=await run(()=>invoke({action:'add_process',...proc}),'Falha ao adicionar processo.');if(r&&!r.duplicate)setProc({...emptyProcess})}
 return <div className="module"><div className="page-head"><div><h1>Tribunais e Processos</h1><p>Tribunal + plataforma judicial + acesso + 2FA + OAB + sigilo processual.</p></div><button className="integration-action" onClick={load}><RefreshCw size={15}/>Atualizar</button></div>{notice&&<div className="integration-notice">{notice}</div>}
 <div className="campaign-tabs process-tabs">{[['connections','Tribunais e plataformas',Landmark],['oab','Importar por OAB',UserCheck],['credentials','Acessos e 2FA',KeyRound],['manual','Adicionar processo',Plus],['status','Monitoramento',ShieldCheck]].map(([k,l,I]:any)=><button key={k} className={tab===k?'active':''} onClick={()=>setTab(k)}><I size={15}/>{l}</button>)}</div>
 {tab==='connections'&&<><div className="integration-panel"><h3><Search size={17}/> Tribunais do Brasil</h3><p>Primeiro selecione o tribunal. Depois informe qual plataforma ele usa para o acesso: PJe, eproc, Projudi, e-SAJ ou outro sistema.</p><div className="input-search"><Search size={15}/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Buscar STF, TJMG, TRT, TRE..."/></div><div className="tribunal-catalog" style={{display:'grid',gridTemplateColumns:'repeat(auto-fit,minmax(260px,1fr))',gap:8,marginTop:14,maxHeight:330,overflow:'auto'}}>{catalog.map(([code,name])=><button className="secondary" key={code} onClick={()=>chooseTribunal(code,name)} style={{justifyContent:'flex-start'}}><Landmark size={14}/><span><b>{code}</b> · {name}</span></button>)}</div></div><div className="integration-panel"><h3><Landmark size={17}/> Tribunal selecionado e plataforma de acesso</h3><div className="integration-form"><label>Código<input value={form.tribunal_code} onChange={e=>setForm({...form,tribunal_code:e.target.value.toUpperCase()})}/></label><label>Tribunal<input value={form.tribunal_name} onChange={e=>setForm({...form,tribunal_name:e.target.value})}/></label><label>Plataforma de acesso<select value={form.judicial_system} onChange={e=>syncPlatform(e.target.value)}>{platforms.map(x=><option key={x}>{x}</option>)}</select></label><label>OAB<input value={form.oab_number} onChange={e=>setForm({...form,oab_number:e.target.value})}/></label><label>UF da OAB<input value={form.oab_uf} maxLength={2} onChange={e=>setForm({...form,oab_uf:e.target.value.toUpperCase()})}/></label><label><input type="checkbox" checked={form.requires_2fa} onChange={e=>setForm({...form,requires_2fa:e.target.checked})}/> Esta plataforma exige 2FA</label><button className="integration-action" onClick={saveConn} disabled={busy}><ShieldCheck size={15}/>Salvar tribunal e plataforma</button></div></div></>}
 {tab==='oab'&&<div className="integration-panel"><h3><UserCheck size={17}/> Importação / monitoramento por OAB</h3><p>A OAB fica vinculada ao tribunal e à plataforma judicial selecionada.</p><div className="integration-form"><label>Tribunal<select value={form.tribunal_code} onChange={e=>{const t=national.find(x=>x[0]===e.target.value);if(t)chooseTribunal(t[0],t[1])}}>{national.map(([c,n])=><option value={c} key={c}>{c} — {n}</option>)}</select></label><label>Plataforma<select value={form.judicial_system} onChange={e=>syncPlatform(e.target.value)}>{platforms.map(x=><option key={x}>{x}</option>)}</select></label><label>Número OAB<input value={form.oab_number} onChange={e=>setForm({...form,oab_number:e.target.value})}/></label><label>UF<input value={form.oab_uf} maxLength={2} onChange={e=>setForm({...form,oab_uf:e.target.value.toUpperCase()})}/></label><button className="integration-action" onClick={saveConn} disabled={busy}><Gavel size={15}/>Salvar monitoramento OAB</button></div></div>}
 {tab==='credentials'&&<><div className="integration-panel"><h3><LockKeyhole size={17}/> Login e senha da plataforma judicial</h3><p>O acesso é cadastrado por <b>tribunal + plataforma</b>. A senha fica no cofre e não volta para a tela.</p><div className="integration-form"><label>Tribunal<select value={cred.tribunal_code} onChange={e=>{const code=e.target.value;const c=connections.find(x=>x.tribunal_code===code);setCred({...cred,tribunal_code:code,judicial_system:c?.judicial_system||cred.judicial_system});setTwo({...two,tribunal_code:code,judicial_system:c?.judicial_system||two.judicial_system})}}>{national.map(([c,n])=><option value={c} key={c}>{c} — {n}</option>)}</select></label><label>Plataforma de acesso<select value={cred.judicial_system} onChange={e=>setCred({...cred,judicial_system:e.target.value})}>{platforms.map(x=><option key={x}>{x}</option>)}</select></label><label>Login / CPF / usuário<input value={cred.login} onChange={e=>setCred({...cred,login:e.target.value})}/></label><label>Senha<input type="password" value={cred.password} onChange={e=>setCred({...cred,password:e.target.value})}/></label><button className="integration-action" onClick={saveCred} disabled={busy||!cred.login||!cred.password}><KeyRound size={15}/>Guardar acesso da plataforma</button></div></div><div className="integration-panel"><h3>Verificação em duas etapas — 2FA</h3><p>O código é usado somente na sessão da plataforma selecionada e não é armazenado.</p><div className="integration-form"><label>Tribunal<select value={two.tribunal_code} onChange={e=>setTwo({...two,tribunal_code:e.target.value})}>{national.map(([c,n])=><option value={c} key={c}>{c} — {n}</option>)}</select></label><label>Plataforma<select value={two.judicial_system} onChange={e=>setTwo({...two,judicial_system:e.target.value})}>{platforms.map(x=><option key={x}>{x}</option>)}</select></label><label>Código 2FA<input inputMode="numeric" value={two.code} onChange={e=>setTwo({...two,code:e.target.value})}/></label><button className="integration-action" onClick={submit2fa} disabled={busy||!two.code}>Usar código nesta sessão</button></div></div></>}
 {tab==='manual'&&<div className="integration-panel"><h3><Plus size={17}/> Adicionar processo manualmente</h3><div className="integration-form"><label>Cliente<select value={proc.client_id} onChange={e=>setProc({...proc,client_id:e.target.value})}><option value="">Sem cliente</option>{clients.map(c=><option value={c.id} key={c.id}>{c.name}{c.cpf_cnpj?` — ${c.cpf_cnpj}`:''}</option>)}</select></label><label>Número CNJ<input value={proc.cnj_number} onChange={e=>setProc({...proc,cnj_number:e.target.value})}/></label><label>Número interno<input value={proc.internal_number} onChange={e=>setProc({...proc,internal_number:e.target.value})}/></label><label>Tribunal<select value={proc.tribunal_code} onChange={e=>{const t=national.find(x=>x[0]===e.target.value);if(t)setProc({...proc,tribunal_code:t[0],tribunal_name:t[1]})}}>{national.map(([c,n])=><option value={c} key={c}>{c} — {n}</option>)}</select></label><label>Plataforma judicial<select value={proc.judicial_system} onChange={e=>setProc({...proc,judicial_system:e.target.value})}>{platforms.map(x=><option key={x}>{x}</option>)}</select></label><label>Parte contrária<input value={proc.opposing_party} onChange={e=>setProc({...proc,opposing_party:e.target.value})}/></label><label>Comarca<input value={proc.comarca} onChange={e=>setProc({...proc,comarca:e.target.value})}/></label><label>Vara<input value={proc.vara} onChange={e=>setProc({...proc,vara:e.target.value})}/></label><label>Área<input value={proc.area} onChange={e=>setProc({...proc,area:e.target.value})}/></label><label>Assunto<input value={proc.subject} onChange={e=>setProc({...proc,subject:e.target.value})}/></label><label>Classe<input value={proc.class_name} onChange={e=>setProc({...proc,class_name:e.target.value})}/></label><label className="wide"><input type="checkbox" checked={proc.is_confidential} onChange={e=>setProc({...proc,is_confidential:e.target.checked})}/> <EyeOff size={15}/> Processo em segredo de justiça / sigiloso</label>{proc.is_confidential&&<label>Nível de sigilo<select value={proc.confidentiality_level} onChange={e=>setProc({...proc,confidentiality_level:e.target.value})}><option value="1">1 — Sigilo</option><option value="2">2 — Restrito</option><option value="3">3 — Segredo de Justiça</option><option value="4">4 — Sigilo intenso</option><option value="5">5 — Sigilo absoluto</option></select></label>}<button className="integration-action" onClick={addProcess} disabled={busy||(!proc.cnj_number&&!proc.internal_number)}><Plus size={15}/>Adicionar processo</button></div></div>}
 {tab==='status'&&<div className="integration-panel"><h3><ShieldCheck size={17}/> Conexões e monitoramento</h3>{connections.length===0?<div className="integration-empty"><Landmark size={28}/><p>Nenhum tribunal configurado.</p></div>:<div className="signature-list">{connections.map(c=><div className="signature-row" key={c.id}><div><strong>{c.tribunal_code} · {c.tribunal_name}</strong><small><b>Plataforma:</b> {c.judicial_system||'não informada'} • OAB {c.oab_number||'—'}/{c.oab_uf||'—'} • 2FA {c.requires_2fa?'sim':'não'}</small><small>Última sincronização: {c.last_sync_at?new Date(c.last_sync_at).toLocaleString('pt-BR'):'ainda não realizada'}{c.error_message?` • Erro: ${c.error_message}`:''}</small></div><span className="status-dot">{c.status}</span></div>)}</div>}</div>}
 </div>}
